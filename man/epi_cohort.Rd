% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{epi_cohort}
\alias{epi_cohort}
\title{A synthetic clinical cohort}
\format{
A data frame with 600 rows and 11 columns:
\describe{
\item{id}{Patient identifier, \code{"P0001"} to \code{"P0600"}.}
\item{age}{Age in years.}
\item{sex}{Factor: \code{"Male"}, \code{"Female"}.}
\item{site}{Factor: \code{"Site A"}, \code{"Site B"}, \code{"Site C"}, \code{"Site D"}.
\code{"Site D"} has no observations.}
\item{treatment}{Factor: \code{"Control"}, \code{"Low dose"}, \code{"High dose"}.}
\item{severity}{Factor: \code{"Mild"}, \code{"Moderate"}, \code{"Severe"}.}
\item{bmi}{Body mass index, kg/m^2.}
\item{crp}{C-reactive protein, mg/L.}
\item{los_days}{Length of stay in days.}
\item{fu_days}{Days of follow-up, to death or to censoring. Censoring is
by dropout or by the end of the study at 365 days.}
\item{death}{\code{1} if the patient died during follow-up, \code{0} if censored.}
}
}
\source{
Simulated by \code{data-raw/epi_cohort.R}.
}
\usage{
epi_cohort
}
\description{
Six hundred simulated patients, shaped like the descriptive tables that
motivate this package: a few continuous measurements crossed with several
categorical variables worth stratifying on.
}
\details{
Two features are deliberate. \code{site} declares a fourth level, \code{"Site D"},
that recruited nobody, so stratifying by \code{site} produces a stratum with
\code{n = 0}; the app lists it and draws no figure. \code{severity} has a small
\code{"Severe"} group, which the minimum-n rule filters out at its default
setting.

The data are simulated. They describe no real patients and support no
clinical conclusion.

\code{fu_days} and \code{death} make the data usable for a Kaplan-Meier curve, and
\code{age} for the categorization controls.
}
\examples{
str(epi_cohort)

# The empty stratum that the app reports with n = 0.
table(epi_cohort$site)
}
\keyword{datasets}
